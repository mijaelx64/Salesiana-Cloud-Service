<Window x:Class="Salesiana.Cloud.ClientUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button x:Name="openFileDialog" Content="Upload" HorizontalAlignment="Left" VerticalAlignment="Top" Width="Auto" Click="Button_Click_1" Height="Auto" Background="White"/>
            <Button x:Name="downloadFileDialog" Content="Download" HorizontalAlignment="Left" VerticalAlignment="Top" Width="Auto" Height="Auto" Background="White" Click="downloadButton_Click"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Bottom">
            <ProgressBar Height="23"></ProgressBar>
        </StackPanel>
        <Grid>
            <ListView Margin="10" Name="fileList">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="220" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Size (Mb)" Width="80" DisplayMemberBinding="{Binding Size}" />
                        <GridViewColumn Header="Modified" Width="150" DisplayMemberBinding="{Binding Modified}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
